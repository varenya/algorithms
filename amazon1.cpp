#include <iostream>
#include <vector>
#include <stdio.h>
#define ll long long
using namespace std;

ll sum_gcd(ll x){
	ll [] gcd = new ll[x+1];
	for(int i=1;i<=x;i++) gcd[i] = 1;
}

int main(void){
}
